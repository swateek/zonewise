{
  "name": "zonewise",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate:cities": "node scripts/generate-cities.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=zonewise"
  },
  "devDependencies": {
    "city-timezones": "^1.3.4",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "wrangler": "^4.24.0"
  }
}
